#include <algorithm>
#include <bits/stdc++.h>
#include <regex>
#include <vector>

using namespace std;
class Solution {
public:
  vector<int> arr;
  vector<vector<int>>memo;
  int helper(vector<int>nums,int start,int end){
  }
  int maxSubArray(vector<int> &nums) {
    if(nums.size()==1){
      return nums[0];
    }

  }
};
